# Docker

This folder contains the container configuration needed to run the FastAPI application locally.

## Build and Run

```bash
cd Docker
docker compose up --build
```

The service will be available at http://localhost:5000.
